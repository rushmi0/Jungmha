<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>
                [%boldRed(%d{dd-MM-yyyy})][%boldMagenta(%d{HH:mm:ss.SSS})] %green([%thread]) %highlight(%-5level) %magenta(%logger{36}) - [%msg]%n
            </pattern>
        </encoder>
    </appender>

    <logger name="org.jungmha.*" level="DEBUG"/>
<!--    <logger name="io.micronaut.http.client" level="TRACE"/>-->
<!--    <Logger name="io.micronaut.context.condition" level="DEBUG"/>-->

    <logger name="org.flywaydb.core" level="DEBUG"/>
    <logger name="io.micronaut.flyway" level="INFO"/>
    <logger name="com.zaxxer.hikari.pool" level="TRACE"/>

    <root level="info">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
